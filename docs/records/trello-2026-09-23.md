# Trello 看板與歷史訂單移轉 — 2026-09-23

使用者指定將委託進度改為 Trello 形式，並確認三個公開看板全部 174 張卡片（含 4 張封存）匯入；歷史卡片沿用原公開名稱，新表單仍匿名。維持原生 HTML／CSS／JavaScript 與 GitHub Pages。

## 資料結果

| 類型 | 匯入 | 排隊 | 草稿 | 已交稿 | 含封存 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 小動圖 | 83 | 35 | 1 | 47 | 0 |
| 角色動畫 | 34 | 4 | 0 | 30 | 2 |
| 貼圖包 | 57 | 2 | 0 | 55 | 2 |
| 合計 | 174 | 41 | 1 | 132 | 4 |

原本 1 筆已交稿的虛構驗收單完整保留，因此正式看板合計 175 筆、已交稿欄 133 筆。移轉前後比較原單內容、通知結果、第 4 版及修改歷史一致。重跑新增 0 筆、跳過 174 筆；歷史訂單通知嘗試均為 0。貼圖來源 55 個 PNG 附件連結只保留於後台。來源缺少聯絡方式、需求、價格與授權，均不補造。

## 已執行驗證

- 58 個離線測試通過，包括既有計價／簽章／通知、30 → 31 欄保留原內容、尾端資料／公式拒絕覆蓋、同名不同 ID、封存、來源排序、重跑不覆蓋管理修改、整批驗證與寫入後失聯、公開資料白名單及 200 筆分頁。
- 真實 GAS 匯入入口由專案編輯者執行；原始匯出、Sheets 備份、匯入批次與完整驗證結果只保存於專案外。移除臨時匯入程式與實際資料後才建立第 3 版，更新既有部署。
- 真實 API 核對 174 張來源卡片的名稱、階段、旗標與封存；匿名管理讀取被拒絕。公開類型篩選為小動圖 83、角色動畫 34、貼圖 58（包含原驗收單），資料不含內部備註、通知名單、付款標籤或附件。
- 本機 Chrome 390×844：已實際顯示 175 筆及各欄件數，沒有整頁橫向溢出。1920×855：確認七欄橫向版面與讀取失敗提示。初次視窗設定未套用時不視為手機驗收，後續以實際 `innerWidth`／`innerHeight` 核對。

## 視覺對照與限制

參考 Trello 的橫向階段欄、件數、堆疊卡片與欄內捲動；沿用本站金棕配色、系統繁中字體及原導覽。採公開唯讀看板，狀態透過管理頁修改，沒有公開拖曳改單。手機保留可左右捲動的欄寬，避免把七欄縮成難讀的窄格。訂單資訊可展開，沒有把未提供的交期或報價塞入卡片。

仍觀察到 GAS 偶發 45 秒讀取逾時；成功請求能返回完整資料，瀏覽器會提示失敗並可重試。曾以不含 charset 的外部探測收到平台 HTML，改用與前端相同的 UTF-8 Content-Type 後完整 API 驗證通過；不能據此斷言所有逾時原因已排除。這次不是持續同步，後續 Trello 異動不會自動更新本站。

新表單收件仍關閉。先前單一收件者的真實 TG 通知與登入已驗證；本次匯入沒有重發 TG。維護步驟見[Trello 匯入](../development/trello-import.md)。

## 上線後補充驗收

GitHub Pages 的 `6daf9dc` 部署成功，正式 390×844 手機介面已驗證選單、類型／階段篩選、空結果、卡片資訊展開及封存標籤；1280×900 桌機已驗證 175／83／34／58 筆切換、擱置 1 筆、已交稿 133 筆及鍵盤橫向捲動，無整頁溢出。根目錄與專案子路徑均能載入看板。4 張封存卡片原本均在排隊欄，依來源保留該階段與封存標記。

使用者追加詢問來源時間後，補充由 Trello Card ID 推得的建立時間、既有最後活動值與匯入時間，另建 GAS 第 4 版。新增測試確認建立時間秒數與讀取不改寫原列；不把這些時間當成交期或正式收件時間。
