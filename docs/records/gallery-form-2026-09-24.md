# 作品排序與表單文案驗收

日期：2026-09-24。此紀錄涵蓋本次六項作品放映室及表單調整；測試使用本機產物與虛構填單資料，尚未發布正式網站。

## 完成內容

- 使用者確認「編號越大越新」，建置依作品 ID 的數字由大到小排列，同號保留來源順序；原精選紀錄不再插入前方。未填入推測的完成日期，作品 ID、原始來源與素材保持原值。
- 放映室文案改為「歡迎走走逛逛，感謝各位委託人的捧場 óωò」。
- 貼圖包隱藏動作與氛圍的準備提示，更新同一角色、免費修改與額外修改條款，參考檔案標籤改為單一角色提醒。
- 聯絡方式原「你的使用者名稱」提示改為「你的使用者 ID，是英文 ID，不是暱稱喔！」，Discord 欄下說明同步更新；素材連結標籤改為「參考檔案、參考素材連結二擇一必填」。

## 實際驗證

- `npm run build`、`npm run check` 通過；包含 94 支作品影片及衍生版、50 筆表單素材、48 款選項、模組及產物一致性檢查。
- `npm test` 共 114 項通過；本次修改的 JavaScript／建置腳本通過 `node --check`，差異通過 `git diff --check`。
- 本機瀏覽器於桌機 1280 × 900、手機 390 × 844 檢查首頁與表單，無橫向溢出，長提示可換行，主控台無警告及錯誤。
- 全部作品從小動圖 57 開始；角色動畫依 37→32 顯示，載入更多延續 31→26；放大角色動畫 37 後按下一件顯示 36。小動圖分類依 57→50 顯示，桌機仍為四欄。
- 貼圖包側欄不顯示動作與氛圍提示，展開說明後可見兩段新條款；切至角色動畫或小動圖時恢復提示及原五檔標籤，切回貼圖包恢復同一角色提醒。
- 使用一款貼圖及虛構 Discord 資料檢查表單：檔案與連結都空白時顯示錯誤；只提供 HTTPS 連結後可進入確認頁，金額及返回修改正常。手機檢查 ID 提示與參考素材標籤完整顯示。

本次未送出正式委託、上傳檔案或傳送通知；五檔上限、計價與第 4 版草稿契約沒有改動。

## 同日後續：展示框比例與留白

依使用者後續要求，`src/pages/home/home.css` 將小動圖展示框改為 1:1、角色動畫改為 16:9，內距由桌機 10px／手機 8px 縮為 6px／4px。各卡片依類型套用，保留完整等比例媒體；直式原檔 `animation-34` 仍保留必要的左右留白。

重新執行 `npm run build`、`npm run check` 與 `git diff --check` 均通過。瀏覽器於 1280 × 900 驗證小動圖四欄及角色動畫三欄，展示框約為 264 × 264、361 × 203；390 × 844 手機分別約為 335 × 335、335 × 189。分類切換與載入更多正常，無橫向溢出或主控台警告／錯誤，作品完整呈現。此為 CSS 調整，未重跑計價測試，亦未發布正式網站。

## 同日後續：預設小動圖分類

依使用者要求移除「全部作品」按鈕與篩選分支，預設選取小動圖，初次顯示八件，保留角色動畫及貼圖分類。無 JavaScript 時仍可直接閱讀全部作品。

`node --check`（`gallery.js`、`build.mjs`）、建置、靜態檢查及差異檢查通過。桌機 1280 × 900 確認預設小動圖、載入更多由 8 件增為 16 件，切換角色動畫顯示 6／37、貼圖顯示 1／1。手機 390 × 844 重新整理後仍選取小動圖、顯示八件且無橫向溢出；放大檢視從小動圖 57 切至 56，主控台無警告或錯誤。尚未發布正式網站。

## 同日發布

使用者授權提交並部署後，重新執行 114 項測試、修改 JavaScript 的語法檢查、正式 `SITE_URL` 建置及靜態檢查，全部通過。

GAS 第 15 版已發布至原部署，固定版本內容與本機產物核對一致；變更限於 `Config.gs`、`Core.gs`、`Import.gs`、`Orders.gs`，網址與 manifest 不變。發布前保存原始碼及部署設定，未執行資料遷移、重新匯入、真實送件或通知。Google 首次切換後讀回舊版本，因此核對固定版本內容後重新指定同一第 15 版，再確認部署版本與網址。

前端隨本次 `main` 提交觸發既有 GitHub Pages workflow；實際發布結果以該提交的 Actions 紀錄為準。部署驗收只讀取正式頁面，拖曳儲存及錯誤處理已使用獲授權的本機虛構委託服務驗證。
