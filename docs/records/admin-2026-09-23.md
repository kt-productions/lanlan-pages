# 管理後台驗收 — 2026-09-23

結果：未通過。使用者要求驗收後台，實際在指定 Telegram 帳號確認成功後，GAS 登入交換回應失敗，未能進入委託工作區。這次沒有修改任何訂單、傳送收件通知或變更登入權限。

## 環境與流程

- 正式頁面：<https://kt-productions.github.io/lanlan-pages/admin/>。
- GitHub Pages 程式版本 `aea3d27`，GAS 第 4 版；Chrome 透過既有瀏覽器工具操作，沒有改用外部 Playwright。
- 實際驗證尺寸為 1920×855。曾要求 390×844，但管理頁讀回仍為桌機尺寸，因此不計入手機驗收；結束時重設尺寸覆寫。
- 目標流程：管理頁 → Telegram 本人確認 → GAS 回程 → 返回後台 → 訂單清單與編輯。
- 實際流程：第一次登入準備失敗；重試進入 Telegram，本人確認後顯示「Telegram 驗證完成」，立即點選「返回管理後台」，最後顯示結果不明訊息，登入面板仍可見、工作區仍隱藏。

## 發現問題

### 登入交換無法可靠完成

使用者已完成 Telegram 確認，回程也成功，但網站沒有取得有效的管理工作階段。這次立即返回後台，不能沿用前次「票證在返回前逾時」解釋本次失敗。

GAS 執行紀錄顯示當次登入準備的 `doPost` 約 1.13 秒即完成；「執行完成」只能證明程式返回，不代表瀏覽器收到有效業務回應。前端依 `createApi` 將逾時、不可讀或非 JSON 回應歸為 `NETWORK`。目前證據指向回應傳遞流程，仍未確定是平台、網路或其他中介環節造成。

另做 4 次不含登入資料的公開讀取探測：一般網址 2 次、不同隨機請求參數的網址 2 次，均未取得有效 JSON。3 次取得 GAS 的 302 回應後在下一站逾時，1 次在首站逾時；已取得的重新導向位置雜湊互異，回應明示 `no-cache, no-store`。沒有證據支持以加入隨機參數修復，因此未將此猜測寫入網站程式。探測每站期限為 20 秒，與網站 45 秒期限分開記錄。

### 登入失敗文案不符合當前操作

畫面顯示「若剛才儲存了變更，請重新載入訂單確認」，但當時還在登入、尚未取得訂單。來源為 `src/pages/admin/index.js` 的共用 `report`，未區分登入、讀取與儲存；應依操作提供可理解的提示。這是次要文案問題，不能以調整提示代替修復登入。

## 檢查結果

| 檢查 | 結果與證據 |
| --- | --- |
| 網址與頁面識別 | 通過，目錄網址 `/admin/`、標題「委託管理｜爛爛 LANLAN」。 |
| 非空白與基本版面 | 通過，導覽、登入說明、按鈕與錯誤區均可見；桌機無整頁橫向溢出。 |
| 建置錯誤遮罩 | 未見，DOM 與桌機截圖無框架或建置錯誤遮罩。 |
| 主控台 | 工具未取得 error／warn 訊息；頁面已捕捉並顯示連線錯誤，不能因此判定登入成功。 |
| 截圖 | 已於對話顯示正式桌機的登入失敗畫面。 |
| Telegram 本人確認 | 通過，GAS 回程顯示驗證完成。 |
| 管理登入與訂單載入 | 失敗，登入面板未隱藏，工作區未顯示。 |
| 搜尋、載入更多、歷史來源與附件 | 受登入阻擋，未實測。 |
| 修改、還原、保存、版本衝突與公開頁同步 | 受登入阻擋，未實測；未寫入原本的虛構測試單。 |
| 通知狀態／重試、登出與手機工作區 | 受登入阻擋，未實測。 |

本次沒有變更網站或 GAS 程式，因此未重跑既有 58 個離線測試；其先前通過紀錄不代替這次正式後台驗收。只更新驗收文件、核對連結與差異，私人探測腳本及結果保存在專案外。

後續需先排除登入回應傳遞失敗，再使用原本的虛構測試單驗證保存／還原與公開頁同步，並唯讀核對歷史訂單的來源、封存及附件；不以停用驗證、延長憑證有效期或修改真實委託來完成驗收。
