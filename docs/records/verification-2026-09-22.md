# 2026-09-22 驗收與文件核對紀錄

此文件保存當日既有瀏覽器驗收及後續文件核對結果。以下文件數、連結數與路徑數是當時分類整理前的統計，不作為目前目錄結構的計數。原瀏覽器紀錄未綁定程式版本，不能據此判定現在的介面全部通過。

目前檢查方式見[驗證指南](../development/validation.md)，現行設計見[介面規格](../design/interface.md)。本次分類整理只搬移、分拆與去除重複說明，不重新執行下列歷史測試。

## 當日文件核對（分類整理前）

- 對照兩頁 HTML、CSS、互動程式、建置／檢查／預覽腳本、計價程式與測試內容，更新現行設計及流程說明。
- 核對作品數量、五筆命名作品、精選排序、表單欄位、計價設定、第 2 版草稿的完整欄位及空值語意。
- 加總 94 支 MP4 的來源紀錄與實體檔案大小，皆為 121,048,684 bytes；當次未重算全部媒體 SHA-256，也未重新播放影片。
- 確認表單素材為 64 筆來源紀錄、63 個不同本機路徑；閱讀既有原表單文字存檔及折扣、交付規格圖片，未重新爬取原表單。
- 執行 `codex plugin list --json` 核對相關安裝與啟用狀態，並讀取專案配置；未驗證 GitHub 帳號、組織權限或外部通知。
- 檢視七張設計概念圖後依使用者指示移除整個概念圖目錄；將設計方向與取捨轉為 `docs/design/interface.md` 的文字紀錄。
- 8 份說明文件（7 份 docs 文件與專案 README）的 Markdown 結構、24 個本機連結、38 個專案路徑及 npm 指令檢查通過；已確認沒有殘留概念圖目錄引用。
- 依使用者移除概念圖的要求，同步 `AGENTS.md` 的設計紀錄準則與插件選擇附錄，改以文字規格維護，並檢查 Markdown 與修改差異。
- 草稿文件的 22 個頂層欄位與 `createSnapshot()` 一致；直接呼叫現有計價函式核對 7 組文件金額範例，包含單人動畫、兩種小動圖方案及全選貼圖的用途／付款組合，均一致。這是文件範例核對，不是完整計價測試或瀏覽器驗收。
- 當時目錄尚未初始化 Git，使用修改前副本進行 `git diff --no-index` 與空白檢查，不將它描述為已有可提交的 Git 差異。

當次不需重建網站，未執行 `npm run build`、`npm run check`、`npm test`、瀏覽器驗收或預覽服務。文件中的目前行為是原始碼核對結果，實際渲染與操作通過的聲明僅限下方瀏覽器紀錄。

## 2026-09-22 既有瀏覽器驗收（歷史紀錄）

以下保留原文件記錄的 2026-09-22 第二版與站內委託表單驗收。當時使用 Codex Browser／IAB，未部署、推送、送出原表單或傳送 Telegram 通知。當次文件核對未重做這些測試；原紀錄未綁定程式版本，不能視為目前所有更新都已驗收。

### 當時的視覺核對

原紀錄曾以首屏、作品區、價目與委託頁四張概念圖核對實際畫面。後續依使用者指示全部移除，設計意圖改記於[介面規格](../design/interface.md)；此處保留當時比較結果，不再提供圖片引用。當時最終色票依指定的 `animation-02.mp4` 取代紫色概念。

| 比較點 | 實際核對與調整 |
| --- | --- |
| 文案與層級 | 首屏標題、簡介、作品／價目與表單標題一致；未添加英雄區標籤或假數據 |
| 版面 | 保留三件首屏動畫、三欄作品牆、條列價目；表單左說明右欄位，手機改直向 |
| 字體與間距 | 系統繁中粗體，標題與控制有明確字級差；長價格與說明在手機正常換行 |
| 色票 | 奶油白 #fffdf4、麥黃 #f5edc5、蜂蜜金 #e4bf54、深可可 #46301f、主棕 #765039；移除舊介面紫色 |
| 原始素材 | 首屏中央改 animation-02；全部作品保持原色、比例與完整角色，不採用概念中的重畫作品 |
| 按鈕與圖示 | 延用箭頭／暫停圖示；價目改為單一站內表單入口，舊 Google 表單連結數為 0 |
| 表單同意 | 概念的預勾選同意不採用，實際直播／作品展示授權均預設未選 |

首屏可見文案核對通過；中央作品編號改為 02。原紫色、生成角色、外連表單及預選同意是刻意調整，分別依使用者指定、內容保真與實際欄位要求處理。沒有尚待修復的明顯裁切、重疊或橫向溢出。

概念原生尺寸 1536×1024；曾在此尺寸檢查原版面，最後一輪 IAB 尺寸覆寫未作用於既有分頁，故以實際 1280×720 桌機與 390×844 手機完成最終核對，未宣稱最終畫面為 1536×1024。

主要文字對比：內文 12.10:1、次要字 5.54:1、金色按鈕 6.98:1、說明區 6.07:1、選取狀態 7.05:1。作品本身不做色彩處理。

### 當時的功能核對

- 94 支動畫皆有 muted／autoplay／loop／playsinline；首屏實測 readyState 4 且播放時間持續增加。
- 作品分類 95／37／57／1、載入更多 6→12、暫停／恢復、檢視器播放／切換／Escape／焦點還原通過。離開可視範圍或開啟檢視器會暫停背景影片。
- 三種委託流程皆能到確認頁；貼圖跨頁選 No.1、48，顯示原價小計 NT$350；缺少款式／必填值會阻擋下一步。
- 參考檔只留本機；有效圖片可預覽，11 MB 檔案在貼圖流程被 10 MB 限制擋下。100 MB 上限已配置，但未以大型檔案或後端上傳測試驗證。
- Facebook 欄位拒絕非 Facebook URL；切換 Telegram 後可以繼續。小動圖不顯示直播同意、商用或備註欄。
- 確認頁正確顯示「尚未送出」，本機 JSON 草稿下載通過；未測試任何外部送件。
- 手機表單／48 款貼圖分頁無橫向溢出，桌機授權選項預設未選；新版瀏覽器 console 無警告或錯誤。
- 原文件記載 `npm run build`、`npm run check`、兩個前端 JS 的 `node --check` 與 `git diff --check` 通過，並記錄 64 筆表單素材來源大小及 SHA-256 一致。當次文件核對未重跑這些功能檢查；目前目錄並無 Git 紀錄，無法重現或確認當時 `git diff --check` 的執行環境。

上述是當時的驗收範圍。後續已確認的 PayPal、小動圖急件與貼圖折扣順序見 [現行表單規格](../reference/commission.md)，不可因歷史紀錄再將它們列成未決費用；仍無正式收件或報價承諾。
