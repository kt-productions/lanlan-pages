# 2026-09-24 作品管理本機驗收

已實作作品草稿、PNG／JPG／GIF／MP4 預覽與上傳、文字修改、換檔、下架、發布狀態與失敗重試，以及 GAS／Drive／GitHub Actions 所需程式。此紀錄只代表本機來源驗證，不代表正式串接已啟用。設定與限制見[作品管理與發布](../development/artwork-service.md)。

## 環境與範圍

- Windows、Node.js 24；沿用既有本機預覽服務，沒有另啟動伺服器。
- Playwright 與已安裝的 Edge，桌機 1440 × 1000、手機 390 × 844；Browser plugin not available，使用既有瀏覽器工具替代驗收。
- 管理 API 以虛構 GAS／Sheets／Drive／GitHub 測試替身回應；沒有讀寫真實委託、上傳正式檔案或傳送 Telegram 通知。
- FFmpeg／ffprobe 處理虛構色塊，Git 分支測試使用本機暫存 bare repo；隔離建置使用複製的來源，不修改正式素材。

## 已通過

| 檢查 | 結果 |
| --- | --- |
| 完整 `npm test` | 149 項通過，其中作品契約、後端及 Git 分支測試共 13 項。 |
| `npm run test:media` | PNG、JPG、GIF、MP4 實際解碼／轉檔、原檔位元組一致、衍生檔可讀、損壞來源拒絕。 |
| `npm run build`、`npm run check` | 現有 95 件展示、94 支來源影片、50 筆表單素材來源與 48 款選項核對通過。 |
| 隔離建置 | 加入四種虛構作品、修改文字、下架舊作品後共 98 件；兩種網址路徑、編號排序、文字跳脫、GIF 複製與 release SHA 正確。 |
| 語法 | 修改的 JavaScript／MJS、16 個後端 GS 檔可解析，3 份 workflow YAML 可解析。未使用 actionlint。 |
| 差異 | 檢查來源差異、未追蹤新檔、空白問題與文件相對連結；沒有改寫舊原檔或加入憑證。 |

後端案例涵蓋管理白名單、停用開關、真實 RSA 簽章、repo 範圍、草稿重送及版本衝突、Drive 寫入成功但回報中斷、檔案格式與雜湊、公開／備份確認、dispatch 失敗保留佇列、跨站簽章與租約隔離、遠端原檔不符時禁止清理，以及清理後發布失敗從 Git 接續。放棄草稿時的清理失敗保留可重試紀錄，較舊衝突工作也可明確放棄。Git 實測包含初次建立、正常快轉、相同／已包含版本的部署重試，以及分歧時不改寫 production。

兩種螢幕尺寸完成作品清單、上傳預覽、草稿、未勾選時阻擋發布、勾選後發布、狀態回報、文字修改、下架、放棄與清理重試，以及委託／作品／收益分頁切換。根路徑與 `/lanlan-pages/` 管理頁皆可重載；頁面沒有橫向溢出，相關主控台與頁面錯誤為零。截圖已檢視確認文字、縮圖及手機操作區可讀。

公開作品集另以隔離產物與實際素材，在根路徑及子路徑驗證 GIF 依減少動態偏好切換靜態縮圖／原動態；PNG／JPG 讀展示圖，MP4 讀展示影片，說明中的 HTML 字串以純文字呈現。上一件／下一件、Escape 關閉、焦點回到原作品及重新整理後的下架結果通過。

## 尚待正式啟用驗證

GitHub App 是否已建立及安裝仍待確認；正式 Secrets／Variables、GAS 作品儲存初始化及 production 切換尚未完成。目前讀到的 `github-pages` environment 分支政策只允許 main，切換前須更新。

需在設定完成後，以核可的虛構作品測試真實 GAS 傳輸、App token 的 dispatch／push、Drive 配額與永久刪除、GitHub 分支保護、Pages deployment 及上線回報。離線替身不能證明這些雲端權限已設定正確。第二個繪師網站的實際跨站隔離、備份還原及帳號總容量管理也未在本次建立。

本次沒有提交或推送此次來源、部署 GAS、建立 production，亦沒有清理正式 Drive。啟用時依維護文件順序進行，避免先切換 workflow 卻尚未設定可發布的分支。
