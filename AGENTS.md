# 專案運作準則 — LanLan Pages

本專案為「爛爛 LANLAN」的個人介紹、作品集與委託價目網站，包含作品集首頁及獨立委託表單。使用原生 HTML、CSS、JavaScript（ES Modules）與 Node.js 靜態建置，沒有 Vue、React、Vite 或付款功能；依使用者 2026-09-22 的委託服務需求新增 Google Apps Script 後端、Google Sheets 訂單儲存與 Telegram 管理員登入。

2026-09-23 使用者已明確授權推送至 `kt-productions/lanlan-pages` 公開倉庫並部署 GitHub Pages；本機預覽仍供開發驗證。委託表單提供驗證、預估、確認、JSON 草稿及可設定的收件；已建立 Sheets 與 GAS 專案、初始化訂單表，並於 2026-09-23 部署 Web App 第 1 版；Telegram 設定仍待完成，目前不收件。參考素材以 HTTPS 連結提供，沒有 Drive 檔案上傳。以下準則適用於本目錄及其子目錄的開發、重構、內容與文件維護。

## 1. 嚴格遵守準則

- 修改前先確認本文件、使用者本次需求及受影響模組的限制；完成前依變更範圍重新檢核，不可為了通過檢查而移除必要限制或修改預期結果。
- 保留本專案的靜態架構、原始作品、來源紀錄、本機預覽及 GitHub Pages 發布流程。不得套用其他專案的資料管線、框架或部署設定。
- 使用者明確更新的需求與決策優先於舊來源及舊文件；發現差異時保留來源、記錄決策，不得自行推定新的價格、授權或發布許可。

## 2. 全盤檢視與交接導向

- 每次修改前閱讀 `README.md`、`content/site.json` 與相關程式碼，完整追蹤受影響的資料、建置與頁面流程，不只修改畫面上出現問題的位置。
- 視覺調整閱讀 `docs/design/interface.md`；作品與素材調整閱讀 `docs/reference/assets.md` 及對應來源清單；表單或計價調整閱讀 `content/commission.json`、`docs/reference/commission.md`、`docs/reference/draft-schema.md`、`src/features/commission/pricing.js` 與相關測試。
- 以新人能接手為目標撰寫註解，解釋「為什麼這樣做」。折扣與加價順序、來源差異、條件欄位、草稿狀態、影片播放與減少動態行為，都應保留必要的業務背景；不為直觀語法增加重複註解。
- 區分已實作功能、來源存檔、待確認事項與未來規劃，不把設計文件中的串接構想描述為已上線功能。

## 3. 規劃優先

- 實作前列出簡要步驟、影響檔案、資料或行為變化及驗證方式；依任務規模規劃，純文件修改不需要擴大成網站重構。
- 變更作品 ID、分類、精選清單、素材路徑、貼圖編號、計價欄位或草稿格式前，先檢查所有讀取端、站內連結、既有來源紀錄與下載草稿的相容性。
- 草稿欄位的名稱、型別、空值語意或適用條件若有不相容變更，須評估 `content/commission.json` 的 `version` 與輸出 `schemaVersion`，同步文件並說明影響，不可悄悄改變同一版本的契約。
- 預估金額、確認頁與下載草稿必須使用同一份計價邏輯；規劃時先確認是否需要同步規則、說明文字與測試。

## 4. 變更檢核與自我修正

- 在 Git 倉庫中完成修改後，必須執行 `git status --short`、`git diff` 與 `git diff --check`；若有暫存變更，亦須檢視 `git diff --cached`。
- 若工作目錄尚未初始化 Git，保留修改前副本，以 `git diff --no-index` 或等效逐檔比較檢查內容與空白問題，並如實說明限制；不得為了完成檢查擅自初始化 Git、建立遠端或提交。
- 確認變更與任務相關，沒有遺留除錯輸出、憑證、個人填單資料、失效連結或無用程式碼。保留使用者既有修改，不覆蓋不屬於本次工作的內容。
- 完成回報應說明修改目的、實際驗證結果及尚未驗證的部分。未執行的測試不可記為通過。

## 5. 強制測試與驗證

依變更風險選擇直接相關的檢查。下列要求可疊加；`npm run check` 會讀取 `dist/`，應先建置，再檢查。

| 變更範圍 | 必要驗證 |
| --- | --- |
| 純 `AGENTS.md`、README 或其他說明文件 | 檢查 Markdown 結構、路徑、指令、文件一致性及差異；不必建置、啟動伺服器或執行計價測試。 |
| HTML、CSS、建置腳本、網站內容 JSON 或靜態素材 | 執行 `npm run build`，成功後執行 `npm run check`；JSON 變更另確認可解析及欄位相容性。 |
| JavaScript／`.mjs` | 對所有實際修改的檔案執行 `node --check <檔案路徑>`；若影響網站產物，亦執行建置與檢查。 |
| 計價、折扣、款式價格、方案或計價輸入欄位 | 執行 `npm test`；依變更補上有意義的邊界或回歸案例，檢查金額上下限、明細加總、選項切換與不適用費用。 |
| 作品、縮圖、表單圖片或來源清單 | 核對檔案存在、作品 ID、分類、尺寸、比例、來源及受影響原檔的大小／SHA-256。`npm run check` 會驗證表單素材雜湊，但不代表已核對所有影片與首頁原始素材的雜湊。 |
| 介面、互動、導覽或表單流程 | 在具備預覽授權後，以桌機及手機尺寸進行瀏覽器驗收，檢查主控台錯誤與下列相關流程。 |
| 預覽伺服器、資源路徑或未來發布設定 | 檢查網域根目錄與 `/lanlan-pages/` 子路徑、四頁直接開啟及重新整理、靜態資源與站內連結；串流邏輯變更另驗證 MP4 Range 請求。 |

- **作品集驗收**：分類、每次載入更多、作品放大、上一件／下一件、全頁暫停／恢復、自動播放受阻與媒體載入失敗、減少動態偏好、鍵盤操作及焦點還原。
- **表單驗收**：三種類型、三步驟、條件欄位、必填、授權選擇、檔案類型及大小限制、貼圖跨頁全選／取消、返回修改、即時計價與 JSON 草稿。切換類型後，不適用欄位不得參與驗證、計價或被誤寫入草稿。
- **版面驗收**：桌機與手機無橫向溢出、遮擋或角色裁切；手機選單、固定金額列、錯誤提示及站內連結可操作。
- `npm test` 執行 `tests/` 中的計價、草稿契約、欄位驗證及建置工具測試，不能代替瀏覽器驗收。檔案大小限制通過前端檢查，也不代表已具備後端上傳能力。
- 純文案或文件不新增與行為無關的測試。需要但尚未取得預覽授權時，先完成其他檢查，再明確列出待驗證項目及啟動原因，不宣稱介面已通過驗收。

## 6. 文件一致性與可追溯性

- 完成前檢視 `AGENTS.md` 與 `README.md`；若未來存在 `CLAUDE.md`，也須檢核一致性。目前沒有 `CLAUDE.md`，不必為了滿足清單新增重複文件。
- 依變更同步相關文件：視覺規格對應 `docs/design/interface.md`，來源與費用差異對應 `docs/reference/commission.md`，草稿契約對應 `docs/reference/draft-schema.md`，串接規劃對應 `docs/plans/form-integration.md`，驗證指引與歷史結果分別放在 `docs/development/validation.md` 與 `docs/records/`。
- 指令、Node.js 版本、環境變數、發布狀態與插件設定以 `package.json`、`scripts/`、`.codex/config.toml` 及實際檔案為準；不記錄不存在的工作流程或外部資源。
- 新增驗收紀錄應註明日期、範圍、實際尺寸及限制。保留歷史紀錄的時間背景，不把舊驗收結果當成本次測試結果。
- 公開文件不記錄個人電腦帳號、絕對工作路徑、暫存備份位置或私人雲端管理連結；使用專案相對路徑與通用設定步驟，實際維運清單保存於專案外。此規則依使用者 2026-09-23 公開前整理要求新增。
- 已確認的費用更新寫入 `content/commission.json` 的規則與 `updates`，尚未確認的來源差異保留於 `unresolved` 或來源盤點；不要為了文件看似一致而改寫原始來源。

## 7. 架構邊界嚴守

前端維持無第三方執行期套件的靜態網站，沿用 Node.js 22 以上及 ES Modules；本次後端簽章驗證使用鎖定的 node-forge，只打包至 Apps Script。新增套件、框架、後端或外部服務必須有當前任務的實際需要，不為套用插件預設而遷移架構。

| 層級／路徑 | 職責與界線 |
| --- | --- |
| `content/site.json` | 網站文案、首頁展示價目、精選排序與社群連結；既有原表單 URL 留作來源，不重新做成送件入口。 |
| `content/works.json` | 作品資料與媒體來源。影片分類使用 `animation`、`chibi`；貼圖總覽目前由建置腳本加入。 |
| `content/commission.json`、`content/forms/` | 目前表單規則與計價設定、原表單存檔及款式資料。區分使用者確認的新規則與來源原文。 |
| `content/source-assets.json`、`content/form-assets.json` | 保留素材的來源、專案相對路徑、大小及雜湊紀錄。 |
| `scripts/`、`scripts/lib/` | 建置、檢查與預覽入口，以及共用的內容讀取、模板跳脫與路徑界線；建置不取得訂單或代為送出表單。 |
| `src/pages/home/index.html`、`src/pages/commission/index.html` | 作品集首頁與獨立委託頁模板；首頁只保留表單入口，不重新內嵌整份表單。 |
| `src/pages/` | 各頁模板、入口腳本與專用 CSS；入口協調功能模組，共用樣式不夾帶首頁專用版面。 |
| `backend/apps-script/`、`build/apps-script/` | Apps Script 來源及獨立打包產物；驗證登入、收件、Sheets 與通知，禁止放入網站 dist。 |
| `src/features/orders/`、`src/pages/admin/`、`src/pages/progress/` | 共用資料契約、API 與管理／進度介面；前端不得作為管理權限的最終判斷。 |
| `src/features/portfolio/` | 作品分類與分批、背景播放、作品檢視器；各模組管理自己的狀態，由首頁入口串接。 |
| `src/features/commission/` | 計價、欄位驗證、檔案預覽、貼圖選擇、草稿與呈現；所有計價集中於 `pricing.js` 的 `estimateCommission()`，不得在畫面或下載流程各寫一套公式。 |
| `src/shared/`、`src/templates/` | 共用導覽、純文字 DOM 呈現與基礎樣式；四頁共用 HTML head、圖示、頁首與頁尾。 |
| `tests/` | 不啟動伺服器的 Node.js 測試；測試資料使用虛構內容。 |
| `public/` | 目前網站使用的素材；未使用與未開放素材依使用者決定移出專案，私人來源副本不打包。 |
| `dist/` | 可重新產生的建置輸出，不直接手改，不作為來源；建置清理範圍只能是本專案的 `dist/`。 |

- 主要導覽維持「首頁、委託表單、委託進度」，管理入口置於頁尾，由建置腳本統一產生；首頁與委託頁各自載入必要腳本與樣式。
- 所有內部資源及跨頁連結須支援 `/lanlan-pages/` 子路徑；維持四頁可直接開啟的靜態結構，不引入 SPA 路由假設。
- 建置頁面為 `dist/index.html`、`dist/commission/index.html`、`dist/progress/index.html` 與 `dist/admin/index.html`；JS／CSS 依來源結構輸出至 `dist/assets/site/`。模組 import 相對於模組檔案，HTML 與動態媒體 URL 相對於頁面，不可混用。
- 維護送件功能時，依 `docs/reference/draft-schema.md` 與 `docs/reference/order-api.md` 確認資料與失敗處理契約；伺服器須重新驗證輸入及計價，前端預估不得成為可信訂單金額。

## 8. 語言規範與在地化

- 所有對話、文件、新增或修改的註解及 Git Commit Message 使用繁體中文（台灣用語）。程式識別字、API 名稱與原始來源文字維持必要原文，不任意翻譯來源存檔。
- 使用「專案、回呼、模組、記憶體、資料庫、伺服器、非同步、資料、API／介面」等台灣常用詞，避免中國用語。
- 需要建立 Commit 時，一律使用 `type(scope): 中文描述`；`type` 依變更選用 `feat`、`fix`、`docs`、`refactor`、`test`、`chore` 等，`scope` 對應實際模組，例如 `docs(agents): 整理作品集協作準則`。
- 每筆 Commit 必須有繁體中文的 Description（commit body），清楚記錄修改原因、主要變更及測試／驗證結果。此格式規範不代表每次任務都要自動 Commit 或推送。

## 9. 素材、計價與表單資料保護

- **保留來源**：原始 MP4、GIF、PNG、來源 URL、來源頁面、大小及 SHA-256 都是可追溯資產。不得以壓縮、裁切、重繪或重新下載的檔案悄悄覆蓋原檔；需要衍生素材時另存並記錄對應來源。
- **保留歷史差異**：依使用者 2026-09-23 公開前整理要求，未開放及未使用素材不保留於公開專案；移除前先將原檔與來源紀錄保存於專案外。公開來源若需節錄，明確標註省略範圍與原因，不把歷史費率改寫成現行規則。
- **內容真實**：作品、價格、聯絡方式與條款以繪師公開來源或使用者明確更新為準。不得自行聲稱開放委託、確定交期、幣別、修改次數、商用授權，亦不得臆測角色名、委託人或創作年份。
- **作品權利與設計紀錄**：不得把 AI 圖片當作繪師作品。設計概念改以 `docs/design/interface.md` 的文字規格與取捨紀錄維護，不保留設計概念圖；正式作品保留原色、比例與角色完整性，不擅自加入開源或作品再利用授權。
- **預估與正式報價**：計價保留上下限、加減價明細及待確認說明。角色動畫幣別目前未確認，不自行補為 TWD；PayPal 加價是本站委託設定，不代表官方收款費率。
- **草稿狀態**：送出前表單資料只存在頁面記憶體，重新整理即清除；依本次需求正式送件後才保存至 Sheets。不擅自新增 localStorage、分析追蹤或 Cookie；管理登入僅暫存 OAuth 綁定值至 sessionStorage，驗證後刪除，管理 token 留在記憶體。JSON 草稿只包含文字與檔案中繼資料，不含參考檔本體。
- **送件界線**：本機草稿固定保留 `mode: "local-preview"`、`submitted: false`、`priceConfirmed: false`、有本機參考檔時 `reference.uploaded: false` 及 `estimatedPrice.confirmed: false`；下載草稿不得顯示為送件、上傳或付款成功。
- **授權與個資**：直播及作品展示授權由填單者明確選擇，不預先勾選。測試使用虛構資料；不將真實暱稱、聯絡方式、角色設定檔或下載草稿存入公開素材、日誌或版本紀錄。

## 10. 開發伺服器、發布與環境限制

- **啟動授權**：未取得使用者明確授權前，不擅自啟動 `npm run dev`、`npm run preview` 或同等預覽服務。需要瀏覽器驗收時，先說明原因；同一工作中已有明確授權者不重複索取。純文件修改不啟動服務。
- **本機預覽**：只綁定 `127.0.0.1`，預設埠為 4173，可由 `PORT` 覆寫。`npm run dev` 先建置再提供預覽；`npm run preview` 只提供既有 `dist/`，修改後需重新建置，沒有自動熱更新。
- **發布授權**：使用者於 2026-09-23 明確要求建立 `kt-productions/lanlan-pages` 公開倉庫、推送並部署 GitHub Pages；以 `main` 的 GitHub Actions 流程發布 `dist/`。此授權不代表開放收件、修改雲端資料分享權限、傳送真實通知或改用其他託管服務。
- **網站網址**：`SITE_URL` 是建置時使用的網址設定，本機預設仍為預覽位置；正式 workflow 從 GitHub Pages 設定取得網址，預期為 `https://kt-productions.github.io/lanlan-pages/`。不得把本機 canonical 或 sitemap 當成正式設定。
- **外部操作**：不得擅自送出原 Google 表單、提交真實委託、上傳參考檔或傳送 Telegram 測試通知；文件中的串接規劃不構成這些操作的授權。
- **憑證安全**：本機預覽不需要 API 金鑰；正式後端設定見 `docs/development/order-service.md`。GitHub 沿用既有 MCP 或 `gh` 登入，不要求另貼 Token。串接所需的 Token、金鑰等敏感資訊不得出現在前端、`content/`、`public/`、`dist/`、除錯輸出或 Commit；後端設定另行管理。

## 11. 程式碼風格與互動品質

- 沿用原生 JavaScript、ES Modules、既有命名與格式，不因「強型別」要求擅自導入 TypeScript 或新的建置框架。複雜資料以 JSDoc、明確結構、輸入檢查與必要測試說明契約。
- 編輯器遵循 `.editorconfig` 的 UTF-8、LF、兩格縮排與檔尾換行。將長 HTML、CSS 規則及多步驟回呼展開，避免把整個表單欄位或函式擠成一行。
- 計價函式保持獨立於 DOM 的純計算，維持以整數分處理金額及明確捨入規則；缺值、未知款式、重複選取與不適用選項都須有明確處理。
- 建置時輸出的 HTML 及內嵌 JSON 必須保留既有跳脫處理。表單輸入優先透過 `textContent` 呈現，不將使用者文字直接拼入 `innerHTML`。
- 使用語意化 HTML、可見焦點、正確標籤與狀態提示；手機選單、檢視器與表單流程需支援鍵盤，切換步驟及關閉檢視器時妥善管理焦點。
- 影片維持靜音、循環、行內播放，保留全頁暫停／恢復與 `prefers-reduced-motion`。避免一次下載全部作品；離開視窗、隱藏分頁及開啟檢視器時妥善暫停背景影片。
- 媒體失敗保留縮圖及可理解的提示；瀏覽器拒絕自動播放時提供手動操作。參考檔預覽及下載使用的 Object URL 必須釋放，不以空的例外捕捉隱藏問題。
- 無 JavaScript 時仍應能閱讀介紹、價目與作品，並直接開啟原始媒體；不要把靜態內容全部改成依賴腳本才能出現。

## 12. 準則鎖定

- 本文第 1–12 節的運作準則僅能由使用者發起變更；AI 不得為了方便實作、省略驗證或放寬授權界線而自行修改。
- AI 可隨已完成的實作補充文末「附錄：已確立的技術決策」，但必須記錄日期、依據及實際檔案；不得把未實作構想寫成既定決策，或藉附錄繞過正文限制。
- 發現準則與實際程式碼不一致時，先查明使用者已確認的需求與變更紀錄，再說明差異；不得自行以改寫準則掩蓋問題。

## 附錄：已確立的技術決策

- **2026-09-24 管理導覽立即顯示**：依使用者要求改善登入後入口延遲，`admin-access.js` 改為依已保存且未到期的登入同步顯示，再於背景呼叫 `auth.session`。暫時無法連線不隱藏入口，登出、到期或明確權限失效仍清除；延遲回應不能恢復已登出狀態。此更新先前「等候驗證才顯示」的呈現決策，只影響公開的管理頁連結，管理資料與操作仍由伺服器獨立驗證，不新增保存資料或延長期限。

- **2026-09-24 僅看板獨立加寬**：依使用者補充，`shared/board.css` 移除 `.board-page` 的寬度覆寫，讓 main、標題、工具列、件數與提示沿用 `.wrap`。1024px 以上僅 `.progress-board` 置中延伸、最大 1920px；窄螢幕保留看板內捲動。此為先前六欄版面的範圍修正，不變更卡片與登入流程。

- **2026-09-24 有效登入的管理導覽**：依使用者要求，四頁 `main-nav` 在本站 Telegram 登入仍有效且通過管理白名單確認後，追加「委託管理」。`admin-access.js` 透過 `auth.session` 輕量驗證，不讀取訂單或延長期限；`navigation.js` 同步登入、登出、到期與跨分頁變更，無法確認權限時隱藏入口。更新前述固定三個導覽項目的決策，頁尾仍不顯示管理入口；管理 API 繼續獨立驗證權限。

- **2026-09-24 管理金額與明細**：依使用者要求，`quote-editor.js` 提供一般訂單明細編輯與整數分加總，Trello 歷史單僅可編輯總額。共用 `contract.js` 驗證管理請求的 `quote`，保存至 `detailsJson.quote`，省略時保留、明確 null 才清除；舊預估、來源與歷史保留，不增加 Sheet 欄位、不公開金額、不發送通知。沿用既有管理權限、revision 與本機虛構資料驗收。

- **2026-09-24 緊湊六欄與卡片編輯**：依使用者後續要求，公開進度移除工作階段選單。`shared/board.css` 於 1024px 以上平均排列六欄，窄螢幕保留看板內捲動；管理卡片不再顯示編號與編輯按鈕，點選或 Enter／空白鍵開啟原視窗。`board-drag.js` 抑制拖曳尾隨點擊，保留移欄儲存、錯誤處理與權限；測試使用已授權的本機虛構資料服務。

- **2026-09-24 首頁中央 GIF**：依使用者指定，中央改用 `逼餔撩髮2.gif` 的 MP4 衍生檔；原 GIF 不覆寫，建置不打包 GIF。`content/hero-video.json` 保存來源與衍生資料，`scripts/optimize-hero.mjs` 產生 960 × 540 影片與縮圖並核對逐格時間戳；沿用原播放控制，不新增作品牆項目或修改既有作品 ID。

- **2026-09-24 作品排序與表單文案**：依使用者確認「編號越大越新」，`scripts/build.mjs` 將作品依 ID 數字由大到小排列，同號保留來源順序；第 7 節所列 `site.json` 精選紀錄保留，但不再控制作品牆排序。`content/commission.json` 與委託頁更新貼圖包單一角色及修改條款、準備提示、聯絡 ID 與素材標籤，計價及第 4 版草稿契約不變。原始來源、作品 ID 與素材不改寫；詳細規格見 `docs/reference/assets.md` 及 `docs/reference/commission.md`。

- **2026-09-24 管理登入保留三天**：依使用者要求修復關閉網頁後需重登並延長 session，更新第 9 節原記憶體登入決策：`admin-session.js` 僅將 token、版本與伺服器到期時間保存於按 API 網址區隔的 localStorage，不保存委託資料；OAuth 綁定值仍使用 sessionStorage。`Auth.gs` 以 Script Properties 保存 token 雜湊對應紀錄，從核發起固定 72 小時，不滑動續期；每次管理操作重新核對白名單，登出、到期及權限失效可撤銷。舊工作階段沿用原期限，前端處理跨分頁登出、儲存受阻及網路中斷。此為本次明確需求對原登入保存限制的更新，不變更表單草稿保存方式。

- **2026-09-23 價格格式與動畫幣別**：依使用者要求，首頁以同字級小字 NT$／起包住價目，貼圖包為 NT$ 50 ~ 250 起。`formatPriceRange()` 統一價格區間為「~」，動畫 `pricing.currency` 確認為 TWD；此為使用者對第 9 節原待確認幣別的後續決策。預估、確認、草稿及新委託共用幣別；`Attachments.gs` 保留舊動畫重試雜湊，既有回執及上傳可接續，歷史資料不批次改寫。詳見 `docs/records/price-format-2026-09-23.md`。

- **2026-09-23 管理登入自動完成**：依使用者要求，`login-popup.js` 在點擊登入時同步開啟 Telegram 視窗，原管理頁以綁定 browserKey 的 `auth.poll` 取得結果後沿用票證交換。GAS 的 iframe 禁止無使用者操作的頂層轉址，因此保留原分頁並自動完成登入；視窗受阻時沿用同分頁手動回程。原有 PKCE、RS256、白名單、兩分鐘票證及記憶體工作階段不變，未增加 OAuth 權限。實作見 `Auth.gs`、`Web.gs` 與管理頁，驗證見 `docs/records/login-popup-2026-09-23.md`。

- **2026-09-23 已交稿按需載入**：依使用者確認，公開進度與委託管理預設只向 API 要求未交稿，已交稿欄位內按鈕才補載已交稿；重新載入回到未交稿。`board-data.js` 取代原 `admin-data.js`，共用範圍分頁與完整性驗證，`Orders.gs` 在分頁前套用 `delivery`；省略參數相容舊前端。封存展示、登入權限、訂單原始資料及通知規則不變。完整契約見 `docs/reference/order-api.md`。

- **2026-09-23 公開文案與管理入口更新**：依使用者明確要求，`src/templates/footer.html` 移除四頁共用的「委託管理」連結，管理員使用 Telegram 通知入口或既有管理網址；此為使用者對第 7 節頁尾入口的後續變更要求。進度介紹合併為單段，排序提示簡化；委託頁依服務選項切換介紹，文案共用 `content/site.json`，由 `scripts/build.mjs` 注入前端資料，保留原生 HTML／JavaScript。

- **2026-09-23 部署快取修復**：正式瀏覽器曾持續執行舊版巢狀模組；建置改用 `dist/assets/site/<內容版本>/`，保留來源目錄與相對匯入。版本由 `scripts/lib/site-assets.mjs` 計算完整 JS／CSS 內容，`scripts/check.mjs` 仍核對來源與產物一致性。此為正文資源目錄下的版本子目錄，不改用框架或其他託管平台。
- **2026-09-23 登入可靠性修復**：依使用者要求排除後台登入失敗，正式 HTTPS 前端改用 GAS Html Service 的通訊頁，`callApi` 與舊 POST 共用管理驗證。通訊限制見 `backend/apps-script/Bridge.gs`、`src/features/orders/bridge.js`；票證在原兩分鐘期限內可由原分頁重取同一登入結果，不建立第二個工作階段或延長有效期。實際部署與驗收結果另記錄，不能以離線測試代替正式後台驗收。
- **2026-09-23 Trello 移轉**：依使用者確認，匯入全部 174 張來源卡片（含封存），沿用歷史卡片公開名稱；新表單仍匿名。公開頁採橫向七欄，API 提供完整件數及最多 200 筆分頁。第 31 欄 `sourceJson` 保存來源，匯入由擁有者私下執行、按卡片 ID 去重、不覆蓋歷史、不發 TG；真實來源與備份不得進入公開倉庫。實作與界線見 `backend/apps-script/Import.gs`、`scripts/lib/trello-import.mjs` 及 `docs/development/trello-import.md`。

以下為 2026-09-22 依現有程式碼、內容設定與使用者決策紀錄整理的交接背景；價格以 `content/commission.json`、款式以 `content/forms/sticker-options.json`、計算以 `src/features/commission/pricing.js` 為準。

- **網站與素材**：作品資料含 37 支角色動畫、57 支小動圖；建置另加入 1 張貼圖總覽。2026-09-23 依使用者要求，公開專案只保留 01–48 款；未開放款式與未使用素材移除，原始副本保存於專案外。視覺依 `animation-02.mp4` 使用奶油白、麥黃、蜂蜜金與可可棕，詳細規格見 `docs/design/interface.md`。
- **貼圖折扣**：12／24／36／48 張分別折 100／250／400／600，只取最高門檻；No.33–35 中選取的第 2、3 款半價，可與數量折扣併用。商用先扣兩種折扣再乘 2。全選 48 款一般用途為 6,600，商用為 13,200。
- **角色與加價**：角色動畫第二角色 +3,000，轉場 +500–1,000，每位角色複雜費 +0–2,000；小動圖第二角色按所選方案原價加 50%，加急固定 +600，每位角色複雜費 +0–200。雙人加價不放大急件與複雜費，範圍內確切金額由繪師確認。
- **PayPal**：三種類型目前統一於完整加減價後小計加收 5%，上下限各自計算並四捨五入至小數兩位。全選貼圖一般用途含加價為 6,930，商用為 13,860；舊表單的 6%／7% 留在來源存檔。
- **草稿契約**：依本次素材連結收件需求升為 `schemaVersion: 3`，新增 `referenceUrl` 並允許無本機參考檔時 `reference: null`；詳見 `docs/reference/draft-schema.md`。正式回執與草稿分開，沒有草稿匯入。
- **插件選擇**：新介面或明確重新設計使用 `build-web-apps:frontend-app-builder`；互動、響應式與渲染驗收使用 `build-web-apps:frontend-testing-debugging` 及現有 Browser／統一瀏覽器工具，沿用本專案原生技術。2026-09-22 依使用者要求移除設計概念圖，設計改以 `docs/design/interface.md` 的文字描述維護。
- **插件維護**：專案工具用途見 `docs/development/plugins.md`，個人安裝狀態另行管理。保留單一有效來源，不直接修改已載入快取；全域變更先備份，不為一般專案任務順手升級插件。此專案目前無須額外加入 Sites、Cloudflare、Vercel、Figma 或資料庫服務。
- **目錄與互動整理（2026-09-22）**：依使用者要求將來源分為 `pages`、`features`、`shared`、`templates`，測試移至 `tests/`。當時草稿維持第 2 版，後續委託服務升為第 3 版；補上檔案切換重驗、繁中必填提示與無腳本手機導覽，架構及驗證證據見 `docs/development/architecture.md`、`docs/records/refactor-2026-09-22.md`。
- **委託服務（2026-09-22）**：依使用者本次明確需求新增 Sheets 收件、Telegram 通知、指定管理員登入與訂單編輯、公開匿名進度。後端為 `backend/apps-script/`，設定與部署見 `docs/development/order-service.md`，契約見 `docs/reference/order-api.md`。此需求更新本文的功能範圍與導覽說明，沒有放寬正式發布或真實通知的授權規則。
- **權限與可靠性**：Telegram OIDC 使用 PKCE、nonce、RS256 公鑰驗證與 `profile.id` 白名單；每次管理請求重新核對。收件鎖定及冪等識別碼避免重複，先存訂單再通知，修改使用 revision 與歷史快照。公開回應不包含聯絡資料、暱稱、素材或內部備註。
- **後端資源啟用（2026-09-22）**：依使用者明確指示新增 Sheets 與獨立 GAS，存於指定的 LanLan Pages Drive 資料夾，使用既有 `@KTProductionsBot`，管理頁網址未定。資源及待辦見 `docs/records/cloud-setup-2026-09-22.md`；初始化入口為 `setupOrders()`，必須檢查 Google 執行身分。後端啟用不代表授權發布前端、開放收單或傳送未指定目的地的通知。
- **公開工作看板（2026-09-23）**：依使用者更正，進度頁供所有人瀏覽所有工作，不使用委託編號查詢或百分比。階段為排隊中、草稿繪製中、草稿確認、等待付款、完稿中、待付尾款、已交稿；`isRush`／`isOnHold` 是可並存的獨立旗標，不變更計價。Sheets 在原 27 欄尾端追加兩欄，保留舊欄與歷史；規格及相容對應見 `docs/reference/order-api.md`。本次僅更新本機來源與產物，雲端升級及部署仍待執行。
- **GAS 後續設定與多人通知（2026-09-23）**：使用者恢復後端設定，指定通知一位或多位 Telegram 數字 ID，管理後台未來放 GitHub Pages，並明確要求待專案確認後再推送。已部署既有 GAS 的 Web App 第 1 版、保存 `WEB_APP_URL`／前端 `apiUrl`，Orders 升為 30 欄；第 30 欄 `notificationRecipientsJson` 保存逐位結果，`TELEGRAM_NOTIFY_USER_IDS` 與 `ADMIN_TELEGRAM_IDS` 分開。保持暫停收件，Telegram 憑證及 ID 名單、HTTPS 管理網址仍待完成。沒有建立遠端、推送或發布 Pages，亦未傳送真實通知。此紀錄更新前述啟用進度，不修改第 1–12 節準則；設定與驗證見 `docs/development/order-service.md`、`docs/records/service-setup-2026-09-23.md`。

- **目錄網址（2026-09-23）**：依使用者要求移除公開網址中的 `.html`；四頁以目錄首頁提供，保留舊入口轉址與 query／fragment，管理回程需相容舊 `admin.html` 與新 `admin/`。

- **2026-09-23 影片分級壓縮**：依使用者要求保留原始 MP4，另以 `scripts/optimize-videos.mjs` 產生長邊最多 640px 的預覽版及 1280px 的展示版，來源與衍生雜湊記於 `content/video-assets.json`。首頁影片可見時載入預覽，檢視器開啟時才載入展示版；縮圖另延遲載入。`scripts/lib/video-assets.mjs` 由建置與檢查共用，現已核對全部影片原始及衍生雜湊；不代表核對首頁原始圖片。規格及維護見 `docs/development/video-optimization.md`。

- **2026-09-23 管理看板與編輯視窗**：依使用者需求，公開進度移除「訂單資訊」展開區；`board-view.js` 與 `shared/board.css` 共用七欄外觀，管理頁卡片另有編輯按鈕。`admin-data.js` 讀完並核對所有分頁後才呈現完整看板；`src/pages/admin/` 以 HTML dialog 提供既有編輯功能及放棄修改確認，保留焦點、行動版捲動與錯誤處理。未改後端契約、GAS 設定或登入權限。

- **2026-09-23 收件啟用**：依使用者明確指示開啟收件，既有 GAS 專案的 `ACCEPTING_ORDERS` 已由 `false` 改為 `true` 並重新讀取確認。沿用第 6 版部署及原通知／管理設定，沒有新增測試訂單或發送通知；本段更新前文歷史收件狀態。詳見 `docs/records/receiving-2026-09-23.md`。

- **2026-09-23 多檔附件**：依使用者要求，參考檔案最多五個、素材連結選填，兩者至少提供一項；草稿升為第 4 版，以 references 陣列保存未上傳中繼資料。送件逐檔上傳至私人 Drive，後端驗證檔案及配額、以預留 ID 保護重試，全部完成才建立訂單。後台提供受管理權限保護的預覽／下載，每檔最多 10 MiB、合計 45 MiB，為 GAS 相簿請求保留空間；依使用者後續要求，Telegram 多張靜態圖片合併為相簿，按收件者與檔案分別保存傳送結果。實作見 Attachments.gs、AttachmentNotifications.gs；正式部署及驗收狀態見 docs/records/attachments-2026-09-23.md。本段更新前述歷史上傳狀態及單檔契約，不修改第 1–12 節。

- **2026-09-23 暱稱與封存**：依使用者更新需求，公開進度顯示委託暱稱；Trello 保留核可的原卡片名稱。新增獨立 `isArchived`，第 32 欄保存本站封存狀態，舊空值沿用來源封存；明確解除後可恢復原階段。公開 API 排除封存，後台「所有工作」及一般旗標篩選也排除，只有「封存」篩選顯示。保留原訂單、來源與更新歷史，不發送封存通知。實作見 `contract.js`、`board.js`、`Orders.gs` 及管理編輯視窗；此為使用者對先前匿名與封存展示規則的更新，不修改第 1–12 節。

- **2026-09-23 完整 Telegram 通知**：依使用者要求，`NotificationText.gs` 將已驗證表單欄位、選項及伺服器預估明細送到既有通知名單。先送純文字，過長分段，圖片保留相簿；第 30 欄 JSON 保存單份文字快照與逐位逐段回執，重試不補已成功內容。沒有新增公開資料、收件對象、權限或 Sheets 欄位，亦不批次補寄舊單；規格見 `docs/reference/order-api.md`。

- **2026-09-24 看板與小動圖排列**：依使用者七項修正，首頁小動圖分類桌機改為四欄及每批八件；`contract.js` 將草稿確認與等待付款合併為 `draft_review`，相容舊 `awaiting_payment` 讀取與更新，保留原值及歷史。公開與管理卡片按原始建立時間由舊到新，Trello 採原卡片建立時間。管理預設小動圖、移除上方階段篩選並支援拖曳移欄，編輯視窗保留可供手機與鍵盤操作的階段選單；急件紅底、擱置藍底、並存加紅色側框。使用者同日授權提交與部署，GAS 已更新至第 15 版，前端沿用 `main` 的 GitHub Pages 發布流程；規格見 `docs/design/interface.md` 與 `docs/reference/order-api.md`。
