# 已交稿依最後更新排序

日期：2026-09-25。範圍：目前工作樹的公開進度、委託管理及共用 API 排序；使用虛構訂單驗證。

## 變更

- 使用者要求已交稿按最後更新時間由新到舊排列。前端看板與 GAS 清單改用 `compareBoardOrders()`，分頁、補載、篩選及編輯後都採相同規則。
- 已交稿使用本站 `updatedAt`，未交稿保留原始建立時間由舊到新。混合 API 清單先未交稿再已交稿，確保兩種排序方向不會交錯；同時間以編號固定排序，缺少或無效時間放在所屬範圍最後。
- 更新進度頁排序提示、README、介面規格、API 契約及 AGENTS 附錄。不改寫既有訂單、Trello 來源、日期或歷史。

## 已完成驗證

- 所有修改的 JavaScript／mjs 通過 `node --check`。
- `npm test`：175 項全部通過，包含 GAS 打包、新增的已交稿跨頁／補載／篩選、相同及無效時間、編輯後移到最上方；既有未交稿建立時間排序仍通過。
- Trello 混合清單舊案例原本要求全部按建立時間排序，依新規則改為核對未交稿同時間的編號順序及已交稿所在範圍，保留件數、篩選、封存及公開資料界線檢查。
- `npm run build`、`npm run check` 通過；前端產物與共用模組一致。
- 檢視 `git status --short`、完整差異與 `git diff --check`。

## 限制與發布

2026-09-25 未取得啟動預覽服務的明確授權，因此當天未執行桌機／手機瀏覽器驗收，沒有驗收尺寸或截圖。不把離線測試當作正式頁面驗收。

2026-09-25 尚未提交、推送或部署 GAS／GitHub Pages。2026-09-26 使用者明確要求部署，先同步遠端已發布的新作品，再重新通過 175 項測試與正式網址建置／檢查；依既有流程備份、核對並更新 GAS，再由 main 驗證、production 發布。實際上線結果以 [Pages Actions](https://github.com/kt-productions/lanlan-pages/actions/workflows/pages.yml) 與正式 `release.json` 的版本核對為準。
