# 有效管理登入的導覽入口

日期：2026-09-24。依使用者要求，首頁、委託表單、委託進度與委託管理的 `main-nav` 在本站 Telegram 登入仍有效、並通過後端管理白名單確認後顯示「委託管理」。桌機與手機共用相同行為。

## 實作

- 建置預設隱藏入口；`admin-access.js` 呼叫 `auth.session` 驗證後才顯示。後端沿用既有驗證，不讀取訂單、不傳回 Telegram 身分、不延長登入期限。
- 登入交換或管理 API 成功可立即確認本頁入口；各頁功能與導覽共用 API client，避免重建 GAS 通訊 iframe。
- 登出、到期及權限失效隱藏入口；跨分頁登入資料變更同步驗證，回到頁面或恢復連線時重驗。網路失敗暫時隱藏但保留憑證；延遲回應不得恢復已登出的入口。
- 原三天期限、管理 API 權限、訂單與通知規則不變。頁尾維持沒有管理入口。

## 驗證

| 範圍 | 結果 |
| --- | --- |
| 自動化 | `npm test`：125／125 通過；修改的 JavaScript 語法檢查、正式網址建置、`npm run check` 與差異空白檢查通過。 |
| 契約與競態 | 訪客不發驗證請求；伺服器確認、到期、撤銷、網路重試、錯誤格式、不續期、無法保存登入及延遲回應均有離線測試。 |
| 桌機 | Codex 內建瀏覽器，1280 × 900；訪客隱藏、有效登入在四頁顯示、目前管理頁標記、跨頁保留、跨分頁登出、無效登入、短效到期與撤銷權限通過。 |
| 手機 | 390 × 844；選單顯示四個入口，首頁點「委託管理」成功導航並收合，頁面無橫向溢出。 |
| 路徑與畫面 | `/lanlan-pages/` 四頁及根目錄的首頁／管理頁、管理頁重新整理正常；頁面有實際內容，無錯誤覆蓋層；桌機與手機截圖已檢視，主控台無錯誤或警告。 |

瀏覽器驗收使用只綁定 `127.0.0.1` 的隔離服務、虛構登入與訂單，後端使用本次實際打包程式及測試服務替身。未重新操作正式 Telegram 授權，也未修改真實訂單或發送通知。

## 發布

既有 GAS Web App 已更新至第 17 版，僅 `Web.gs` 改變；固定版本內容、正式部署版本及原網址均已重新讀取核對。沒有變更 Sheets 或管理白名單。前端沿用 `main` 推送觸發 GitHub Pages，部署結果與正式資源核對於本次任務回報。
